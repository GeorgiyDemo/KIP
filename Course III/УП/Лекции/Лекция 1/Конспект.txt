class a{
	private: - закрытые поля (по-умолчанию)
		int z,x;
	public: - открытые поля
		int y;
		void set(int n,int m){
			x=n; z=m;
		}
		void print(){
			cout>>x>>" ">>z;
		}

	protected: - защищенные поля
		int x;   закрытое, но оно наследуется и наследник может им пользоваться
}

int main(){
	A ob1, ob2;
	ob1.y=5;
	ob2.x=6;

	int n, m;

	coun>>n>>m;
	ob2.set(n,m);
}

Наследование - механизм, при котором классы наследуют свойства;
Пример: 
Птицы => Ястреб
Птицы => Синица

Открытые поля - получаем доступ через .
К закрытым мы так обратиться не можем. 

Конструктор - это функция, запускаемая сразу после создания объекта
Деструктор - 

