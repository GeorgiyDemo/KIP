ob.show(); //пример
ob.sum(myclass ob2);
Указатель this - это указатель, который передается при вызове функций.
Это указатель на объект, который передается вызываемой функции. Так она понимает, с каким объектом работает.

